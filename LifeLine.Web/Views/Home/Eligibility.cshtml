
@{
    ViewBag.Title = "Eligibility";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Eligibility</h2>

@*<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script type="text/javascript">

    $(document).ready(function () {
        $.ajax({
            url: "/api/api/Eligibility",
            type: "Get",
            success: function (data) {
                for (var i = 0; i < data.length; i++) {
                    $("<tr><td>" +  data[i].title + "</td></tr>").appendTo("#questions");
                }
            },
            error: function (msg) { alert(msg); }
        });
    });
</script>

<div id="body">
    <section class="content-wrapper main-content">
        <table id="questions" class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayName("Title")</th>
                </tr>
            </thead>
        </table>
    </section>
</div>*@

<div id="bodyContainer" ng-app="EligibilityApp">
    <section id="content">
        <div class="container">
            <div class="row">
                <div class="flip-container text-center col-md-12" ng-controller="EligibilityCtrl" ng-init="nextQuestion()">
                    <div class="back" ng-class="{flip: answered, correct: correctAnswer, incorrect:!correctAnswer}">
                        <p>
                            <button class="btn btn-info btn-lg next option" ng-click="nextQuestion()" ng-disabled="working">Next Question</button>
                        </p>
                    </div>
                    <div class="front" ng-class="{flip: answered}">
                        <p class="lead">{{title}}</p>
                        <div class="row text-center">
                            <button class="btn btn-info btn-lg option" ng-repeat="option in options" ng-click="sendAnswer(option)" ng-disabled="working">{{option.title}}</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

@section scripts {
    <script src="~/Scripts/angular.js"></script>
    <script src="~/Scripts/EligibilityController.js"></script>
}